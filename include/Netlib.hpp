#pragma once

#include <iostream>
#include <string>

namespace netlib {

namespace network {

namespace tcp {
std::string simple_test_1(const std::string& s) { return std::string(s); }

}  // namespace tcp
}  // namespace network
}  // namespace netlib
